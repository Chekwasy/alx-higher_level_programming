#!/usr/bin/python3
"""Check for subclass"""


def inherits_from(obj, a_class):
    """The function to check the subclass"""

    if (type(obj) == a_class):
        if (issubclass(obj, a_class)):
            return True
    else:
        return False
