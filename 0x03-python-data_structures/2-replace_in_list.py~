#!/usr/bin/python3
def replace_in_list(my_list, idx, element):
    j = 0
    for i in my_list:
        if j == idx:
            if i < 0:
                return my_list
            else:
                mylist.insert(idx, element)
                return my_list
            j += 1
    return my_list
